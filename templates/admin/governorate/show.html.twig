{% extends 'base-admin.html.twig' %}

{% block title %}Governorate{% endblock %}

{% block body %}
    <h1>Governorate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ governorate.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ governorate.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('governorate_index') }}">back to list</a>

    <a href="{{ path('governorate_edit', {'id': governorate.id}) }}">edit</a>

    {{ include('admin/governorate/_delete_form.html.twig') }}
{% endblock %}
