{% extends 'base-admin.html.twig' %}

{% block title %}Plan{% endblock %}

{% block body %}
    <h1>Plan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plan.name }}</td>
            </tr>
            <tr>
                <th>DescriptionCourt</th>
                <td>{{ plan.descriptionCourt }}</td>
            </tr>
            <tr>
                <th>DescriptionLong</th>
                <td>{{ plan.descriptionLong }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ plan.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plan_index') }}">back to list</a>

    <a href="{{ path('plan_edit', {'id': plan.id}) }}">edit</a>

    {{ include('admin/plan/_delete_form.html.twig') }}
{% endblock %}
